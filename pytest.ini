[pytest]
pythonpath = .
markers =
    unit: isolated domain/model/projection tests with no HTTP server boundary
    integration: API/auth/websocket tests that cross service boundaries in-process
    e2e: end-to-end user flows through the public companion API contract
