# Deprecated: use scripts/install_extension_runtime.sh instead.
# The helper installs a systemd RuntimeDirectory= drop-in and derives the
# runtime directory ownership from the hermes-gateway service account.
